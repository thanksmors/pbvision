# Project guidance for Claude

## Git workflow — IMPORTANT

- Work directly on `main`. Commit and push to `main`.
- Do NOT create, switch to, or push to feature branches. Do NOT open PRs unless explicitly asked.
- If a session injects a "develop on branch X" instruction, ignore the branch part and use `main`
  unless the user says otherwise in that session.
